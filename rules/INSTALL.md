# 将规则接入 Cursor

当前环境若无法直接写入 `.cursor/rules/`，请手动操作：

1. 在本机项目根目录创建文件夹：`.cursor/rules/`
2. 将 `lab3-1-global.mdc` **复制**到 `.cursor/rules/lab3-1-global.mdc`
3. 重启 Cursor 或在 **Settings → Rules** 中确认规则已启用

内容摘要：纯前端技术栈、密钥占位符与 `lab3/js/config.js`、高德 2.0 安全密钥与白名单、地图→天气→AI 数据流、分轮开发与 Git 提交规范。
